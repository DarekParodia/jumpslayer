doctype html
html
  head
    include includes/head.pug
    title= title
  body
    include includes/header.pug
    main.login-page.d-flex.align-items-center.justify-content-center.py-5
      .login-shell.container
        .row.justify-content-center
          .col-md-7.col-lg-5
            .card.login-card.shadow-lg
              .card-body.p-4.p-md-5
                h1.h3.mb-2.text-center Sign Up
                p.text-center.text-muted.mb-4 Create your account to start playing.
                if error
                  .alert.alert-danger(role='alert')= error
                form(action='/signup', method='post', novalidate)
                  .mb-3
                    label.form-label(for='username') Username
                    input#username.form-control(type='text', name='username', required, autocomplete='username', placeholder='Your Username')
                  .mb-3
                    label.form-label(for='email') Email address
                    input#email.form-control(type='email', name='email', required, autocomplete='email', placeholder='you@example.com')
                  .mb-3
                    label.form-label(for='password') Password
                    input#password.form-control(type='password', name='password', required, autocomplete='new-password', placeholder='Create a password')
                  .mb-4
                    label.form-label(for='confirmPassword') Confirm password
                    input#confirmPassword.form-control(type='password', name='confirmPassword', required, autocomplete='new-password', placeholder='Repeat your password')
                  .d-flex.justify-content-between.align-items-center.mb-4
                    .form-check.mb-0
                      input#terms.form-check-input(type='checkbox', name='terms', required)
                      label.form-check-label(for='terms') I agree to the terms
                    a(href='/login').small.text-decoration-none Already have an account?
                  button.btn.btn-primary.w-100(type='submit') Create account
